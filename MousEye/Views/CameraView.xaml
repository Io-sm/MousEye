<UserControl x:Class="MousEye.Views.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:MousEye.ViewModels"
             d:DesignHeight="700" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:CameraViewModel}">

    <Grid>
        <TextBlock Text="{Binding Message}" Background="LightGray" TextWrapping="Wrap" Width="380" Margin="10,554,410,61" />
        <Image Source="{Binding Test}" Width="380" Height="240" Margin="10,10,410,0" VerticalAlignment="Top" IsHitTestVisible="False" />
        <Image Source="{Binding Test2}" Width="380" Height="240" Margin="410,10,10,0" VerticalAlignment="Top" IsHitTestVisible="False" />
        <Image Source="{Binding Test3}" Width="380" Height="240" Margin="10,309,410,0" VerticalAlignment="Top" IsHitTestVisible="False" />
        <Image Source="{Binding Test4}" Width="380" Height="240" Margin="410,255,10,0" VerticalAlignment="Top" IsHitTestVisible="False" />
        <Button Content="Start" IsEnabled="{Binding IsStartEnabled}" Command="{Binding StartCommand}" HorizontalAlignment="Left" Height="43" Margin="524,500,0,0" VerticalAlignment="Top" Width="148" />
        <Slider x:Name="ThresholdSlider" Value="{Binding Threshold}"  HorizontalAlignment="Left" Minimum ="0" Maximum="1" Height="25" Margin="447,584,0,0" VerticalAlignment="Top" Width="303" />
        <Label Content="{Binding ElementName=ThresholdSlider, Path=Value}" HorizontalAlignment="Left" Height="36" Margin="502,548,0,0" VerticalAlignment="Top" Width="200" />
    </Grid>
</UserControl>